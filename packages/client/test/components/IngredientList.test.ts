import { describe, it } from 'vitest'

describe('IngredientList placeholder', () => {
  it.todo('renders ResourceRow entries for each ingredient with secured indicators')
  it.todo('supports filtering, inventory quick-add actions, and delete safeguards')
})
