{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
            "build": "../../node_modules/.bin/tsc --build ../../tsconfig.build.json",
    "dev": "../../node_modules/.bin/nodemon --watch ./ --ext ts --exec ../../node_modules/.bin/tsx index.ts",
    "lint": "../../node_modules/.bin/eslint .",
    "tsc": "echo 'TypeScript checking is handled from the root workspace'",
    "test": "../../node_modules/.bin/vitest run",
    "prisma:generate": "docker compose -f ../docker-compose.dev.yml exec prisma npx prisma generate --schema=./prisma/schema.prisma",
    "prisma:generate:ci": "npx prisma generate --schema=./prisma/schema.prisma",
    "prisma:migrate": "docker compose -f ../docker-compose.dev.yml exec prisma npx prisma migrate dev --schema=./prisma/schema.prisma",
    "prisma:migrate:ci": "prisma migrate deploy",
    "prisma:reset": "docker compose -f ../docker-compose.dev.yml exec prisma npx prisma migrate reset --schema=./prisma/schema.prisma",
    "prisma:studio": "docker compose -f ../docker-compose.dev.yml exec prisma npx prisma studio --schema=./prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "pg": "^8.14.1",
    "prisma": "^6.5.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.1",
    "@types/pg": "^8.11.11",
    "nodemon": "^3.1.9",
    "pkg": "^5.8.1",
    "tsx": "^4.19.3"
  }
}
